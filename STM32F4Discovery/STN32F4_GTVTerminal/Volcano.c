/* Includes ------------------------------------------------------------------*/
#include "Volcano.h"

/* Private typedef -----------------------------------------------------------*/
/* Private define ------------------------------------------------------------*/
/* Private macro -------------------------------------------------------------*/
/* External variables --------------------------------------------------------*/
/* Private variables ---------------------------------------------------------*/
/* Private function prototypes -----------------------------------------------*/
/* Private functions ---------------------------------------------------------*/

void VolcanoGameLoop(void)
{
  uint32_t x,y;

  x=0;
  y=0;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=1;
  y=20;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=2;
  y=40;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=3;
  y=60;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=4;
  y=80;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=5;
  y=100;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=6;
  y=120;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  x=7;
  y=140;
  while (x<480)
  {
    Line(x,y,x,y+20,1);
    x+=8;
  }

  while (1)
  {
  }
  /* Clear screen */
  Cls();
}

